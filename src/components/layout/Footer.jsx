export function Footer() {
  return (
    <footer className="border-t border-base-600 mt-auto">
      <div className="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between text-xs font-mono text-base-400">
        <span>
          processing happens entirely in your browser — files are never uploaded
        </span>
        <span>v1.0</span>
      </div>
    </footer>
  );
}
